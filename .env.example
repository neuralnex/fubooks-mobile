# Production or local API origin. No spaces around `=`.
# Standalone APK: `expo.extra.apiUrl` in app.json is the fallback when this wasn’t inlined at build time.
EXPO_PUBLIC_API_URL=http://localhost:3000
